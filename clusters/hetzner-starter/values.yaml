# Per-cluster ArgoCD values overlay for hetzner-starter

cloudProvider: "hetzner"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  monitoring: true
  grafanaOAuth: true
  cnpg: true
  valkey: true
  nats: true
  typesense: true

grafanaOAuth:
  allowedDomains: "masena.com"
