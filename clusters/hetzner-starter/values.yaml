# Per-cluster ArgoCD values overlay for hetzner-starter

cloudProvider: "hetzner"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  monitoring: true  # Requires create_backup_bucket = true in Stage 1 for Loki
