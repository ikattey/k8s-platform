# Per-cluster ArgoCD values overlay for gcp-starter

cloudProvider: "gcp"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  # cnpg: true
  # dragonfly: true
  # typesense: true
  # nats: true
  # velero: true
