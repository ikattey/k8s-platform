# Per-cluster ArgoCD values overlay for gcp-starter

cloudProvider: "gcp"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  cnpg: true
  valkey: true
  nats: true
  typesense: true
  velero: true
