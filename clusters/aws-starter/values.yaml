# Per-cluster ArgoCD values overlay for aws-starter

cloudProvider: "aws"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  # cnpg: true
  # dragonfly: true
  # typesense: true
  # nats: true
  # velero: true
