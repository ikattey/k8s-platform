# Per-cluster ArgoCD values overlay for aws-starter

cloudProvider: "aws"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  cnpg: true
  valkey: true
  nats: true
  typesense: true
  velero: true
