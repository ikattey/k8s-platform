# Per-cluster ArgoCD values overlay for aws-starter

cloudProvider: "aws"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  # cnpg: true    # Also requires cnpg_enabled = true in addons terraform.tfvars
  # dragonfly: true
  # typesense: true
  # nats: true
  # velero: true
