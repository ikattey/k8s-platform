# Per-cluster ArgoCD values overlay for ovh-starter

cloudProvider: "ovh"
clusterIssuer: "letsencrypt-production"

components:
  demoApp: true
  cnpg: false
  valkey: false
  nats: false
  typesense: false
