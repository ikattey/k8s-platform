# Per-cluster ArgoCD values overlay for ovh-starter

cloudProvider: "ovh"
clusterIssuer: "letsencrypt-production"
issuerSolver: "dns01-cloudflare"

components:
  demoApp: true
  cnpg: false
  valkey: false
  nats: false
  typesense: false
