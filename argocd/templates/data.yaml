{{- if .Values.components.cnpg }}
{{- $backupProvider := default "s3" .Values.objectStorage.provider }}
{{- $backupScheme := ternary "gs" $backupProvider (eq $backupProvider "gcs") }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg-cluster
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  sources:
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      path: values/cnpg/cluster
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - values.yaml
          - values-{{ .Values.cloudProvider }}.yaml
          - $values/clusters/{{ .Values.clusterName }}/cnpg-values.yaml
        {{- if or .Values.cnpgBackupBucketName .Values.cnpgServiceAccountAnnotationValue }}
        parameters:
          {{- if .Values.cnpgBackupBucketName }}
          - name: backup.destinationPath
            value: {{ printf "%s://%s/postgres/" $backupScheme .Values.cnpgBackupBucketName | quote }}
          - name: backup.provider
            value: {{ $backupProvider | quote }}
          {{- if and (eq $backupProvider "s3") .Values.objectStorage.endpoint }}
          - name: backup.endpointURL
            value: {{ .Values.objectStorage.endpoint | quote }}
          {{- end }}
          {{- if and (eq $backupProvider "s3") .Values.objectStorage.region }}
          - name: backup.s3.region
            value: {{ .Values.objectStorage.region | quote }}
          {{- end }}
          {{- end }}
          {{- if and .Values.cnpgServiceAccountAnnotationValue (eq .Values.cloudProvider "aws") }}
          - name: cluster.serviceAccountAnnotations.eks\.amazonaws\.com/role-arn
            value: {{ .Values.cnpgServiceAccountAnnotationValue | quote }}
          {{- end }}
          {{- if and .Values.cnpgServiceAccountAnnotationValue (eq .Values.cloudProvider "gcp") }}
          - name: cluster.serviceAccountAnnotations.iam\.gke\.io/gcp-service-account
            value: {{ .Values.cnpgServiceAccountAnnotationValue | quote }}
          {{- end }}
        {{- end }}
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: database
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
{{- end }}
---
{{- if .Values.components.valkey }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: valkey
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  sources:
    - repoURL: https://valkey.io/valkey-helm/
      chart: valkey
      targetRevision: {{ .Values.versions.valkey | default "0.9.3" }}
      helm:
        releaseName: valkey
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/values/valkey/values.yaml
          - $values/values/valkey/values-{{ .Values.cloudProvider }}.yaml
          - $values/clusters/{{ .Values.clusterName }}/valkey-values.yaml
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: cache
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
{{- end }}
---
{{- if .Values.components.typesense }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: typesense-cluster
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  sources:
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      path: values/typesense
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - values.yaml
          - values-{{ .Values.cloudProvider }}.yaml
          - $values/clusters/{{ .Values.clusterName }}/typesense-values.yaml
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: search
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
{{- end }}
---
{{- if .Values.components.nats }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nats
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "4"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apps
  sources:
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      path: values/nats
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - values.yaml
          - values-{{ .Values.cloudProvider }}.yaml
          - $values/clusters/{{ .Values.clusterName }}/nats-values.yaml
    - repoURL: {{ .Values.repoURL }}
      targetRevision: {{ .Values.targetRevision }}
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: messaging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
{{- end }}
